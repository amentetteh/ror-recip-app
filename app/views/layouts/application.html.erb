<!DOCTYPE html>
<html>
  <head>
    <title>RorRecipeApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="<%= user_signed_in? ? '' : 'mainbg' %>">
      <% if user_signed_in? %>
      <div><%= render partial: "shared/menu" %></div>   
      <% end %>
      <div class="main"><%= yield %></div> 
  </body>
</html>
 